# third_party

Vendored third-party code lands here. There is nothing here yet.

Prefer normal crates.io dependencies through the workspace `Cargo.toml`.
This folder is only for code that cannot be published to crates.io or that
must be pinned as an exact source snapshot.
